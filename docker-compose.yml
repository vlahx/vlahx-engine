services:
  dev-blog:
    build: .
    image: fastapi-blog:3.11-bookworm
    container_name: dev-vlahx
    user: "1000:1000"
    restart: unless-stopped
    ports:
      - "8002:8000"
    environment:
      APP_PORT: "8000"
      APP_RELOAD: "true"
    volumes:
      - ./app:/app/app
      - ./db:/app/db
      - ./main.py:/app/main.py
      - ./run.py:/app/run.py
    networks:
      - s366

networks:
  s366:
    external: true
